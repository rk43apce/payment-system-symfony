framework:
    test: true
    session:
        storage_factory_id: session.storage.factory.mock_file

monolog:
    handlers:
        main:
            type: stream
            path: '%kernel.logs_dir%/test.log'
            level: error
